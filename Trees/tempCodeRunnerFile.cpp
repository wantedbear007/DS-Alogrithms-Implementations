out << "Enter number of children for " << root -> data << ": " ; cin >> children;
    // for (int i = 0; i < children; i++) {
    //     treeNode * child = new insertElement();
    //     root->children.push_back(child);
    // }